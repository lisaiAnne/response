
    laydate.render({
        elem: '#layer'
        ,position: 'static'
    });
    laydate.render({
        elem: '#layer'
        ,position: 'static'
        ,lang: 'en'
    });
    laydate.render({
        elem: '#layer'
        ,type: 'month'
        ,position: 'static'
    });
    laydate.render({
        elem: '#layer'
        ,type: 'time'
        ,position: 'static'
    }); 
